interface:
  display_name: "Nestly · Conectar e verificar"
  short_description: "Confirme conexão e acesso real ao Nestly"
  default_prompt: "Use $nestly-connect-verify para verificar minha conexão sem alterar nada."

dependencies:
  tools:
    - type: "mcp"
      value: "nestly"
      description: "Nestly account connection; OAuth authorization required"
      transport: "streamable_http"
      url: "https://nestly.bot/mcp"
